# prepend path for homebrew directory
export PATH="/usr/local/bin:$PATH"

# set vi keybindings
set -o vi

# differentiate files from folders
alias ls="ls -F"

# not sure if I need this
export EDITOR=vim
